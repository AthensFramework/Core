<span class="text-field {{ classes|join(' ') }}">
    <input type="text" class="slashless-date-entry" name="{{ slug }}" value="{{ initial }}" placeholder="{{ placeholder }}">
</span>
{% include "field/errors.twig" %}