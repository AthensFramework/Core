<div id="{{ id }}" class="section-container {{ classes|join(' ') }}" data-request-uri="{{ requestURI }}">
    <div class="section-writables">
    {% for writable in writables  %}
        {{ writable|write|raw }}
    {% endfor %}
    </div>
</div>