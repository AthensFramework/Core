{% if method == "JS" %}
    <button class='form-action' onclick="{{ target|raw }}">{{ label }}</button>
{% else %}
    <input class='form-action' name='submit' type='submit' value='{{ label }}'>
{% endif %}