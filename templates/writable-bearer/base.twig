<span id="{{ id }}" class="writable-bearer-container {{ classes|join(' ') }}" data-request-uri="{{ requestURI }}" {{ data|writedata|raw }}>
    {% for writable in writables  %}
        {{ writable|write|raw }}
    {% endfor %}
</span>