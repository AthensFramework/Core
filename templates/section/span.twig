<span {% if id %}id="{{ id|escape('html_attr') }}"{% endif %} class="section-container {{ classes|join(' ')|html_attribute }}" data-request-uri="{{ requestURI|escape('html_attr') }}" {{ data|writedata|raw }}>
    <div class="section-writables">
    {% for writable in writables  %}
        {{ writable|write|raw }}
    {% endfor %}
    </div>
</span>