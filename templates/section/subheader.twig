<h2 {% if id %}id="{{ id }}"{% endif %} class="header-container {{ classes|join(' ') }}" data-request-uri="{{ requestURI }}" {{ data|writedata|raw }}>
    <span class="section-writables">
        {% for writable in writables  %}
            {{ writable|write|raw }}
        {% endfor %}
    </span>
</h2>